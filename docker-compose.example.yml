services:
  immich-edit:
    image: immich-edit:local
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - edit-cache:/cache
    restart: unless-stopped

    # --- AMD GPU (uncomment) ---
    # devices:
    #   - /dev/dri:/dev/dri
    # group_add:
    #   - video
    #   - render

    # --- Intel GPU (uncomment) ---
    # devices:
    #   - /dev/dri:/dev/dri
    # group_add:
    #   - video
    #   - render

    # --- NVIDIA GPU (uncomment) ---
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]

volumes:
  edit-cache:
